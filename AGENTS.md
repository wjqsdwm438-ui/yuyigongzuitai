# 语义规则工作台开发约定

- 本目录是独立源码仓库，不是业务项目或临时输出目录。
- 用户可见目录采用中文-英文双语；skills 等常见约定保留。说明、提示、规则中文为主，必要英文术语附中文解释。Python 等工具自动生成的目录名除外，不改第三方包内部路径。
- 保留第一版工作台全部能力；完整规则原子化、世界模型、经验复用属于目标，不得以存储字段或机械测试代替语义验收。
- 先看 README.md 和说明-docs。复用历史实现前核实原文件及其限制。
- 旧项目只读；建设授权不是业务规则批准。禁止自动接受 VocBench 待审条目。
- 历史证据与当前来源校验分开；历史版本不能被当前内容覆盖。推断关系与已记录事实分开。
- 不读取浏览器密码，不把凭据或真实材料提交 Git（版本管理）。安装限定项目隔离环境，不改全局配置。
- 对外发布、付费调用、生产规则写入需另行明确授权。

## 自然语言使用入口（必须实际执行，不能只讲命令）

- 优先走工作台总入口：先识别Agent设计、原子化、世界模型、变更、诊断、回查或验证任务，再选择执行分支。下列案例流程仅适用于案例诊断，不是所有任务的强制前置。
- 智能体选路并记录依据，程序负责能力交接、产物结构和来源校验；专项任务必须实际交付设计/模型/改稿，不能以列出模块或创建模板冒充完成。

- 用户提交材料并要求工作台处理时，读取`skills/语义规则工作台-workbench/SKILL.md`，按其“实际使用闭环”执行。助手负责生成输入文件、调用中文命令、审阅和导出，不让用户填写内部编号或JSON（结构化数据）。
- 案例诊断分支先用`处理`或`续接`获得真实先例与上下文，再作语义判断；不可跳过检索后声称已复用经验。其他分支不强制查案例。
- 案例分支完成后执行`提交审阅`与`导出报告`；设计、原子化、建模、改稿和验证分支执行`提交专项`与`专项报告`。返回对应产物和边界，仅完成登记或选路不能称完整处理。
- 用户明确“只读／不保存”时不运行写入流程，保持只读分析。普通询问用法也不自动登记案例。
- 用户问历史决定时用`任务列表`定位，再`回查`；不靠对话记忆重编当时依据。
- 所有业务命令使用`--项目 <当前业务项目根>`，不要因程序位于本仓库就把其他项目归属到本仓库。默认检索严格分库；跨项目只读参考需用户明确要求。
