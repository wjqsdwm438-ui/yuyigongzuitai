# 语义规则工作台开发约定

## 项目边界

- 本目录是独立源码仓库，不是业务项目或临时输出目录。
- 用户可见目录采用中文-英文双语；`skills` 等通用约定保留。说明、提示、规则中文为主，必要英文术语附中文解释；不改第三方包内部路径。
- 保留第一版工作台全部能力；规则原子化、世界模型和经验复用必须接受语义验收，不得以字段存在或机械测试代替。
- 旧项目只读；建设授权不是业务规则批准。禁止自动接受 VocBench 待审条目。
- 历史证据与当前来源校验分开，历史版本不能被当前内容覆盖；推断与已记录事实分开。
- 不读取浏览器密码，不把凭据、真实业务材料或运行产物提交 Git。安装限定项目隔离环境，不改全局配置。
- 对外发布、付费调用和生产规则写入需要另行明确授权。

## 正式规则入口

- [README.md](README.md)只负责安装、使用和能力概览。
- [工作台核心 Skill](skills/语义规则工作台-workbench/SKILL.md)是任务选路、专项交付、案例闭环和语义维护方法的唯一正文来源。用户提交材料并要求工作台处理时，必须读取并实际执行该 Skill，不能只讲命令。
- [当前实现状态](说明-docs/当前实现状态.md)记录已实现和未实现边界；[仓库治理标准](说明-docs/仓库治理标准-repository-governance.md)是文件分类、预演、移动、恢复和删除门禁的唯一规则来源。
- `源码-src/语义工作台/dispatch.py`保存程序实际支持的能力名称、组合限制和执行状态。文档与程序冲突时，暂停依赖该冲突判断的动作并核实，不自行选择一个版本；可独立完成的工作继续。

## 调用与项目隔离

- 任务选路、是否建立专项及调用方式按核心 Skill 执行；程序能力名称、组合限制和执行状态以 `dispatch.py` 为准。
- 所有业务命令使用 `--项目 <当前业务项目根>`；不要因程序位于本仓库就把其他项目归属到工作台。默认严格分库，跨项目只读参考需要用户明确要求。
- 案例诊断先检索真实先例并取得必要上下文；检索为空如实记录，继续基于现有证据分析，不编造先例。历史决定通过 `任务列表` 和 `回查` 获取，不靠对话记忆重编。
- 用户明确只读或不保存时，不创建记录或提交专项。

## 修改与验证

- 提出反驳或修改建议前，本地可复现问题先做最小测试，未知外部事实先联网查证可靠来源。授权与条件具备时当轮验证并报告结果，不以“仍需测试”代替执行；无法验证时说明具体阻碍，未证实的判断只作假设。
- 复用历史实现前核实原文件及限制。
