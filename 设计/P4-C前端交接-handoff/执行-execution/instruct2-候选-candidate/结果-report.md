# P4 / instruct2 候选：交付与自动核验结果

日期：2026-09-12。执行：按用户方案"固定赵欣参考音频和原文，使用 instruct2 引导整体讲述方式"，与已验收 zero-shot 版本（98.72 秒）单变量对照。本轮新增 TTS 调用 6/6（交付额度，预批于本执行目录 `run_instruct2.py` 记录），修复额度未动（0/4 之外新增 0）。未修改模型源码；指令仅为调用参数。

## 固定与变化

| 项 | 值 |
|---|---|
| 接口 | `inference_instruct2`（唯一变化；`frontend_instruct2` 按官方实现移除 LLM 参考语音 token） |
| 指令 | `You are a helpful assistant. 请用自然流畅的普通话讲解，语速适中，短语之间衔接紧凑。<|endofprompt|>` |
| 参考音频 | 原始完整 `E:/zimu/谢军/赵欣.WAV`（SHA256 `cce494c6…82808f`，未改） |
| 正文/分段 | 已审阅 405 字符受控稿；批准块清单 `frontend-dry-run.json`（24f53814…）逐字复用 |
| 其他 | `text_frontend=True`、`stream=False`、`speed=1.0`、每块后 0.2s、`zero_shot_spk_id=''`、`seed=1986`（每块前重设） |
| 未混入 | 换气/强调标签、RL 权重、静音过滤、参考更换、标点/分块调整 |

## 产物

- 完整音频：`8d20619a61bc4be188243d2d29f263b1-instruct2-完整第一自然段-full-paragraph.wav`（24kHz 单声道 PCM16，86.76 秒，SHA256 `a46b515747867e71d5f6146a01524794bb2e9dedbf0fa2d710a39957402317e8`），六块逐样本拼接核对通过；请求 ID `8d20619a61bc4be188243d2d29f263b1`，总耗时 149.97s。
- 六个分块 WAV、调用记录 `instruct2-delivery-request.json`（已追加进 TTS 副本 `request_records.jsonl`，第 6 行）、设备证据：llm/flow/hift/f0 全部 `cuda:0`（f0 float64）。

## 自动核验（先于听审）

- **Qwen 独立内容证据（冻结工具、cuda）**：4/4 成功；两正常样本误报 0、已知坏样本仍被拦截（漏报 0）；候选全文按冻结归一与目标**一致，未解差异 0**（`独立核验-qwen/instruct2_full.qwen3.json`）。
- **协议01（whisper-small）**：21 处同音类替字候选 + 6 处重复嫌疑——该工具在两个正常样本上本就 2/2 误报（已冻结记录），仅作旁证，不作为内容结论。
- **停顿/节奏（同一测量口径，与 98.72s 基线对比）**：

| 指标 | zero-shot 基线 | instruct2 候选 |
|---|---|---|
| 总时长 | 98.72s | 86.76s（−12.1%） |
| 停顿数 ≥0.18s | 52（31.6/分） | 30（20.7/分） |
| 停顿总时长 | 16.83s | 10.42s |
| **目标词组内停顿 ≥0.25s** | 管理会计实务 1、业财融合 1、大数据工具实操 1、企业真实场景实战 3+、数据赋能经营 1、企业全链条业务场景邻接 | **全部 0** |

词组级定位（候选内，whisper 词级时间戳）：管理会计实务 0–0.44s / 35.52–37.16s；企业全链条业务场景 33.24–35.30s；业财融合 39.48–40.60 / 67.52–67.62 / 75.70–76.20s；大数据工具实操 65.62–66.62s；企业真实场景实战 69.10–70.92s；数据赋能经营 84.04–84.46s——**内部均无 ≥0.25s 停顿**。残留最长停顿为顿号/句读处 0.30–0.54s（如 20.26s 句号后、8.26s 顿号后），属句读节奏，不再落在词组内部。

## 边界与待办

- 本轮自动化只证明：内容一致 + 词组内停顿消失 + 整体节奏更紧。**音色是否仍是赵欣、韵律是否自然、41–42s 会计读音是否仍读呲，必须由用户听审**；41–42s 局部拼音纠音尚未实施（本候选正文未加任何新控制标记）。
- 若听审通过：将同一调用配置覆盖其余投诉位置前，先按方案对完整段落重交付；若音色偏离或出现新问题，弃用候选，回退 zero-shot 参数（本目录记录了完整调用配置，恢复即切回）。
- 外层包装记录：`qwen-run-receipt-instruct2.json` 如实记录了外层 ExitCode 不可得（Start-Process 句柄未强制求值），以工具输出的 batch_summary 为准；属观察层瑕疵，非工具失败。
