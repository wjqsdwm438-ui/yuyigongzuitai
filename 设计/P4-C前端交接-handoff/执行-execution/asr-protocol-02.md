# P4 独立内容核验证据协议02

适用dispatch-02/acceptance-02；仅补现有音频证据，TTS新增额度0，协议01与原始结论保持原样。

- 应用`C:/Users/admin/.agents/skills/qwen3-asr-local/SKILL.md`；唯一模型入口为`C:/Users/admin/.codex/tools/qwen3-asr-batch.ps1 -Backend transformers -RequireCuda`，一次模型进程处理同一输入目录的四份逐字节副本：当前完整98.72秒WAV、指定人声参考、历史有限接受g1、已知坏12-j第2句。副本SHA256必须分别等于原件，不裁剪、不变速、不提供目标文本提示。
- 固定既有`qwen3-asr`环境及本地`Qwen3-ASR-1.7B`；包装器原参数：Chinese、bfloat16、max_new_tokens=1024、max_inference_batch_size=8、CUDA必需。是否使用FlashAttention按既有加载器能力判断，不安装、不改环境、不换后端/模型/设备重试。
- 进程范围内只使用包装器既有批处理；其原实现若遇批次异常自动拆分，会如实保留batch_summary中的attempt_batch_sizes/batch_splits，不冒称没有内部重试。外部只启动一次包装器。离线环境标志开启，缓存/临时路径限本执行目录；最多600秒，到时停止本次自建进程树，未完成记工具错误，不重试。
- 运行成功须包装器退出0且四份txt/json/report均非空，身份匹配、model_device为cuda；同时保留原始候选、实际工具源码身份、输入及输出身份、参数、退出码与批次摘要。原始候选不可被比较文本覆盖。
- 比较沿用协议01的NFKC、确定Windows繁转简及标点空白处理，数字/否定/英文不增加容差。逐项核对原16差异及新工具全部差异；不新增同音白名单。新识别直接支持源文时，可作为该处内容嫌疑解除的独立证据；仍有未解字词、数量/顺序或新嫌疑则非零。ASR一致不能证明音素、声调、自然度。
- 同次检查两正常/一坏样本，保留正常误报、坏样本漏报及小样本边界；若已知坏样本被自动放行，不能用该工具放行当前成品。若一次本地核验失败/证据不足，停止并据实回主线程，不再扩建核验系统。
- 复用`verify.py`核对当前源、WAV及原始两套ASR证据；stdout仅输出等级/退出码/差异数/证据路径，详细JSON保留。终端执行须显式`exit $LASTEXITCODE`传递验证退出码。

本次结果：实际模型批量一次、4输入/4成功、batch_size=4、batch_splits=0，包装器54.313秒退出0，模型cuda:0。两正常样本归一后全文一致（误报0/2），已知坏样本仍显示大段内容异常（漏报0/1）。当前完整音频的独立转写与目标正文按冻结归一完全一致，原16个替字区间均获源文字词直接支持，新工具没有额外英文/重复或其他未解内容差异；仅放行内容门槛与用户听审，不判读音/声调/韵律。

入口问题保留在`qwen-entry-error.json`与对应日志：此前一次包装器前置检查因PowerShell 5误读无BOM脚本中文，扫描0输入、model_device=not_loaded，未加载/调用模型。修正本目录脚本UTF-8 BOM与进程退出码读取后，才发生上述唯一四音频模型批量；环境、模型、后端与参数均未改变。该零输入入口错误不冒称模型校准。
