# P4 / acceptance-04：首轮A+B授权与验收

日期：2026-09-12；本文件发布后不覆盖。派单范围只到首轮和用户盲听，不扩到后续复验、会计纠音或完整收口。

## 本轮授权

用户本任务最新指令明确要求修订文档、必要实现、A+B首轮共4次GPU合成及内容核验/盲听交付，取代旧交接稿中尚未授权首轮的状态。主线程据本机缺失官方过滤及同接口指令对照的新诊断，同意恢复这一批受控实验；不抹去历史失败。

本批为一次专项对照实验，最多4个实际TTS模型块，四条件逐次执行，不是完整交付额度，也不将每组两次冒称默认“每迭代1次”的普通修复。用户此次明确专项授权覆盖这4次及其执行日的追加额度；既有当日修复实耗4和历史5请求/12模型块保留。本批只能使用4次：崩溃/中断已发调用照计，未发不计，失败不返还，不重试补满。跨日仍使用同一本批剩余额度，不按日期再获4次。

## 固定输入及修订

继承最终执行方案§2的具体路径与身份；精确选取frontend-dry-run.json中字段index==5的唯一元素（当前数组下标4），并断言source_span=[245,319)、74字符、engine_control_ids为空。源坐标取段落plain_text时减source_span起点2，不能把TN长度当源长度。已有完整WAV和块5WAV均保留，不替代本轮A0/B0。

执行前修订最终执行方案与审核文档：非Git副本使用当前执行目录下显式备份路径与补丁前/后/恢复后三点SHA；提交仅限真实Git仓库可提交工件。补齐调用链/实际权重与帧率、每次seed/前端字段、N份盲标及N+3内容核验、缺工具时的停止点、辅助阈值与用户历史报告值来源区分。原调研稿、dispatch-01/02/03及旧证据不覆盖。

## 四条件与门槛

| 项 | 对应R | 必须满足 |
|---|---|---|
| 条件 | R5、R6、R7 | A0零样本无过滤；A1零样本上游最终集合连续最多5个；B0/B1均无过滤instruct2中性/定向；所有条件seed1986、同原块5、同参考、空speaker缓存、同TN/速度/采样。不得修改公开HTTP入口。 |
| 实施 | R7、R10 | 两llm_job分支/继承调用及构造器检查；最小离线过滤反例通过；显式源码备份及可恢复检查；修改源码版本后新进程，实际导入身份可核对；各TTS算子CUDA。 |
| 前置 | R6、R7 | 输入身份、无模型检查、同实际前端dry-run一块、测量工具可用及基线参数预先固定，均在TTS前完成；不可用记证据不足/工具错误并停相应结论链，不为凑结果生成无可测音频。 |
| 原始证据 | R7 | A原始token可回查，A1保留token和被删索引；保存实际权重目录/版本/帧率/前端/条件/seed/算子；两个参考接口差异如实记录；5token不等于音频停顿≤0.2秒。 |
| 内容核验 | R7 | 复用协议02实际本地Qwen入口/参数，本批音文清单显式另绑定：N有效输出+原两正常/一坏对照，N=0不空跑；缺对不判效果、不为凑7新增合成。保留旧协议与旧verify能力，新增批次使用同一个verify入口的明确选择。 |
| 测量与听审 | R5、R6 | 新音频重新定位；记录词内/列举/必要句间停顿和语速；≥0.25秒/>0.35秒仅辅助；可靠测量不足不作该组效果结论。内容通过后仅呈现不含条件身份的N份盲标/随机顺序，用户负责声音判定。 |
| 交付/恢复 | R2、R7、R10 | 唯一恢复点保留历史及本批实耗；恢复本次源码差异，三点身份与备份可核；verify实际执行非旧passed，stdout紧凑；源码与文档按明确允许文件提交，真实材料/音频/原始识别/备份不入Git。 |

A/B结论只能是改善、无改善、退化、未复现、证据不足；用户未听前不自行填改善。因果条件或内容失败停止该组，其他独立有效组可继续；共享模型/身份/测量/环境问题先停止受影响执行。预算用完或用户停止即停，P5始终不放行。复验、会计纠音、组合验证和收口本批新增授权均为0。
