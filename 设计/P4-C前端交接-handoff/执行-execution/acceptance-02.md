# P4 / acceptance-02

本次只补现有音频内容证据；acceptance-01全部验收项仍有效，不替代用户声音结论，不修改历史校准或输出。范围为第一自然段，P5仍未解锁。

## 固定输入

- 当前完整音频：同目录`0c9fbfbd47d44e2bbdd0a45049dd0f3b-完整第一自然段-full-paragraph.wav`；以已保存实际请求和verify校验身份，不生成替代音频。
- 当前目标、源映射及六块清单：同目录`frontend-dry-run.json`，SHA256 `24f53814c0f934171631f2e9c232f4e6abc34db6795edc817333220f5e9b261f`。
- 原始识别与未解差异：同目录`asr-delivery.json`、`verification-result.json`。
- 好坏校准样本及预期：同目录`asr-calibration.json`引用的两正常/一坏音频；沿用原样本身份。

## 逐项条件

| 项 | R | 通过条件 |
|---|---|---|
| 独立内容证据 | R7 | 现有Qwen3-ASR离线包装器一次模型进程批量覆盖当前完整音频及原好坏对照；保存原始候选、实际工具参数和身份。不给目标正文作识别提示。工具失败不换环境循环重试。 |
| 差异判断 | R5、R7 | 逐项对照新旧ASR，明确可支持的内容判断及未解项；ASR一致不证明目标音/声调/自然度。禁止新增同音白名单吞掉差异，数字、否定、英文保持原门槛；仍不能判定则退出非零。 |
| 校准边界 | R7 | 独立工具必须覆盖正常与已知坏样本，保留误报/漏报结果；不因当前音频必须通过而调参。只有实际证据足以消除内容嫌疑才可交听审。 |
| 可恢复验收 | R2、R7 | 复用verify.py重核输入/音频/证据身份及适用检查，stdout仅紧凑摘要，详细结果留JSON；恢复-state.md覆盖写并保留全部TTS历史计数。 |

初版协议01保持原样；新方法及参数落`asr-protocol-02.md`，本次完成后随执行文档显式提交，不纳入真实音频或原始文本。
