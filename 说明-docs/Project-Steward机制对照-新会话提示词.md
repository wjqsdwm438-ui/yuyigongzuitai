# Project Steward 单独讨论提示词

请专项研究 Project Steward，并与本地语义规则工作台 `E:\yuyigongzuitai` 做机制级对照。

背景：Project Steward 当前成熟度不足，不应预设整体安装或直接采用；但它的目录治理、生产者与消费者追踪、生命周期分类、动作分权、冲突停止、存储治理和根因收口等机制可能值得吸收。

请重点判断：

- 哪些机制解决了工作台目前缺失的实际整理与执行问题；
- 哪些能力与现有功能真正重复，哪些只是表面相似；
- 哪些机制适合原生引入，哪些需要改造，哪些应拒绝；
- 是否应替换工作台尚未成熟的功能；
- 如何治理退役规则、历史代码、测试材料、散落文件和上下文污染；
- 如何让模型长期执行统一标准，同时保留具体追问、预演、恢复和删除授权边界；
- 每项建议需要什么真实样本和验证才能成立。

先做只读分析和候选设计，不直接安装、复制整个 Skill 或修改工作台。涉及既有决定时，指出具体旧决定、拟调整内容和证据；至少用一个真实目录样本验证机制价值，再提出是否实施。

## 当前定版状态

此提示词的“是否应替换工作台尚未成熟功能”已经完成裁决：工作台原生仓库治理模块是唯一执行器，已替换早期隔离试用中的 `organize-tool`；`治理-governance/`只是清单、预演和回执目录。Project Steward 不作为依赖、执行器或治理真源，只允许把确有增量的字段补入现有清单。Repomix 的仓库内容打包职责独立保留，不属于本提示词的替换对象。后续不得以本提示词重新启动完整工具选型。
