# 跨项目使用

业务命令显式携带 `--项目 <业务项目根>`，不要因程序位于工作台目录就把其他项目归属到工作台。

默认按项目分库。跨项目参考必须显式请求，结果只读，不导入规则，不授予生产权限。候选提交不等于业务批准。

应用层分库不等于操作系统访问隔离。独立评测的标签应由真实隔离条件保护，不能把同账户下的另一个目录称为密封。

兼容试点需要另备本地数据；真实案例一次性录入脚本和部署记录未随发布提供。安装全局入口、外部 API 及业务写回需另外授权。
